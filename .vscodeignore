.vscode/**
.github/**
src/**
test/**
scripts/**
examples/**
**/*.ts
**/*.map
esbuild.js
tsconfig.json
eslint.config.mjs
pnpm-lock.yaml
ROADMAP.md
.gitignore

# Everything runtime is bundled into dist/ by esbuild.
node_modules/**

!dist/**
# …but not source maps (the !dist re-include above would otherwise pull them in).
dist/**/*.map
